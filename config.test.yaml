# Test Configuration - Uses BTC-USD which is always available
api:
  key_name: "xxxxx"
  private_key: "xxxx"

trading:
  product_id: "BTC-USD"  # Using BTC for testing - it's always available
  dry_run: true  # DRY RUN - no real orders
  poll_interval: 5

  trailing_stop:
    threshold_percentage: 3.0
    trail_percentage: 10.0

logging:
  verbose: true
